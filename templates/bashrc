# ~/.bashrc
set -e
####
#
# .bashrc
#
# This file may get updated when running an install or update, please use
# the config files to modify PMS
#

if [ "$PMS_DEBUG" -eq "1" ]; then
  echo "[DEBUG] PMS Loading ~/.bashrc"
fi

if [ "$PMS_DEBUG" -eq "1" ]; then
  echo "-=[ PMS ]=-"
  echo "PMS:         $PMS"
  echo "PMS_DEBUG:   $PMS_DEBUG"
  echo "PMS_REPO:    $PMS_REPO"
  echo "PMS_REMOTE:  $PMS_REMOTE"
  echo "PMS_BRANCH:  $PMS_BRANCH"
  echo "PMS_THEME:   $PMS_THEME"
  echo "PMS_PLUGINS: $PMS_PLUGINS"
  if [ -d $PMS ]; then
    echo "Hash:        $(cd $PMS; git rev-parse --short HEAD)"
  else
    echo "Hash:        PMS not installed"
  fi
fi

source $PMS/pms.sh bash

if [ "$PMS_DEBUG" -eq "1" ]; then
  echo "[DEBUG] PMS Completed Loading ~/.bashrc"
fi

